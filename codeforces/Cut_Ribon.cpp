#include <bits/stdc++.h>
using namespace std; 
int main (){
    int n,a,c,b;
    cin>>n>>a>>b>>c;
    int maxi=0;
    int veces=0;
    vector<int> cortes;
    cortes.push_back(a);cortes.push_back(b);cortes.push_back(c);
    cout<<maxi;
}